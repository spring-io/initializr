machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - pip install awsebcli
  override:
    - mvn -DskipTests clean dependency:go-offline

test:
  override:
    - mvn clean install
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;

deployment:
  production:
    branch: master
    commands:
      - eb use initilaizr-gr8conf --profile default
      - eb deploy --profile default