machine:
  java:
    version: oraclejdk8

dependencies:
  pre:
    - pip install awsebcli
  override:
    - mvn --fail-never dependency:go-offline || true

test:
  override:
    - mvn clean install

deployment:
  production:
    branch: master
    commands:
      - eb deploy --profile default